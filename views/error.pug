extends layout

block content
    h1= error.message
    h2= error.status
    pre= error.stack
    a(href='/') Go back to porfolio web page


    //- h1 Oops, something went wrong
    //-     article.grid-container
    //-         .grid-x

    //-             h1= `${error.message}: ${error.status}`
